# Реализуй задачу 3: оператор создаёт и двигает заявку

## Цель

Менеджер Protostar (основатель) может завести заявку и сдвинуть статус вперёд
без пульта `/c/` и без логина заказчика. Канал — **только** закрытый D-008.

## Блокер

D-008 закрыт. Если открыт — не пиши код.

## Read set

`docs/api-contracts.md`, `docs/decisions.md` D-005/D-007/D-008/D-014,
hexagon + Prisma skills. Не смотри demo-conductor Нордщита как образец UI.

## TDD

Red: нет маршрута/CLI, кабинет только seed. Green: create возвращает plaintext
секрет один раз; advance идёт только вперёд по ленте; без секрета оператора —
отказ; заказчик по-прежнему только GET.

## Не делать

Экран пульта, cookie, OTP, amo, Telegram, сброс «как live З-10046», CORS POST
для браузера заказчика.

## Приёмка

Given секрет оператора, When create, Then кабинет открывается по выданному
секрету в `accepted`. When advance до `invoice_issued`, Then лента на GET
обновлена. Given чужой операторский секрет, Then не 200.
