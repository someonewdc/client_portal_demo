# Реализуй задачу 2: HTML-листы файлов примера

## Цель

Имена файлов на кабинете открывают `/r/{secret}/d/{fileName}` с таблицей
`files[].specLines` того документа (опрос / КП / счёт). Без download бинаря.

## Зависимости

Задача 1 green. Не выдумывать вторую заявку.

## Read set

Как задача 1 плюс текущие Vue листа, `request-file-display`, DTO `files`.

## TDD

Red: нет листа или таблица берёт `request.specLines` вместо файла. Green: три
kind (если в D-004 три файла) различимы составом строк.

## Не делать

Upload с диска, PDF, задача 3, театр, смена enum статусов.

## Приёмка

Given кабинет примера, When клик по имени файла, Then HTML-лист в том же окне,
спека документа 2–5 строк, не пустая и не копия чужого kind.
