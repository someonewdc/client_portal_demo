# Приёмка всего демо

Отмечать да/нет на стенде после фичи 8. Источник сценариев — `docs/demo-scenarios.md`.
Пункты с h1 статуса, подписями полей, «что дальше», 390px и выпиской файла —
целевая приёмка после UX-задач 1–20 (`выполни ux задачу N` / `реализуй ux задачу N`).
До задач 1–12 стенд закрывает фичи 10–13; сетка и ссылки в покое — задачи 13–18;
иерархия документа — задачи 19–20.

## Показ

- [ ] `/` открывается на `http://localhost:3000`
- [ ] На индексе заголовок «Ссылки для показа»
- [ ] На индексе явный текст: список не показывается заказчику
- [ ] Есть подпись в духе «так выглядит то, что вы отправили бы заказчику в мессенджер»
- [ ] Есть инструкция: нажмите строку — откроется экран заказчика по ссылке
- [ ] Видны 5 сидов, разные контрагенты и статусы
- [ ] Клик по строке ведёт в кабинет без ручного копирования ссылки
- [ ] Кабинет З-10043: «Статус заявки», h1 «КП готово», номер, фраза про ссылку без входа,
      подписи заказчик/изделие/обновлено, фраза «что дальше» про КП, лента 1–4 до
      «КП готово» с датами и «ещё нет» на счёте, спецификация не-SKU, КП как запись;
      под файлами — «Имя файла открывает выписку на экране.»; имя — ссылка на HTML-лист,
      «К заявке» возвращает в кабинет
- [ ] Без hover имя файла и «К заявке» — цвет, линия 2px и лёгкая плашка (`.document-link`), не рамка
- [ ] На индексе номер и title — тот же язык ссылки; штамп — чернила на `--color-rule`,
      выглядит ненажимаемым (`cursor: default`), не ссылка; текущий шаг ленты — тот же штамп
- [ ] Имена файлов З-10044 и даты ленты З-10044 на 1280px делят общие колонки
- [ ] Дата обновления видна и подписана, штампа статуса у даты нет
- [ ] На 390px лента и спека читаются без обрезки заголовков и без горизонтального скролла
- [ ] `/r/this-secret-does-not-exist` — тупик, не форма входа
- [ ] За 30 секунд зритель может сформулировать: «вот что увидит заказчик по ссылке»

## Живой показ (после фич 29–33)

- [ ] `/start` — h1 «Подать заявку», два абзаца D-052, кнопка «Подать заявку»
- [ ] Клик ведёт в кабинет З-10046 со статусом «Принят»; повтор с «Счёт выставлен» снова «Принят»
- [ ] HTML `/start` не содержит секрет пульта
- [ ] На индексе блок «Живой показ»: «Как заказчик начинает» → `/start`, «Пульт смены шага» → `/c/{secret}`
- [ ] Четыре служебные фразы индекса D-027 дословны; видны 5 каталожных сидов
- [ ] Пульт: h1 «Пульт показа», кнопки «Продвинуть по статусу» и «Сбросить»; чужой `/c/nope` — тупик 404
- [ ] На кабинете нет кнопок пульта; живая заявка показывает фразу про обновление; З-10043 — нет
- [ ] Каталог З-10041…З-10045 не меняется от advance/reset

## Границы (всё должно быть «нет»)

- [ ] Нет каталога / корзины SKU
- [ ] Нет логина, OTP, телефона, чата
- [ ] Нет mock-api и mock-core
- [ ] Нет мёртвых кнопок и ссылок `#`
- [ ] Нет кнопки «скачать», которая ничего не скачивает
- [ ] Нет админки смены статусов на кабинете заказчика
- [ ] Нет логина / парольной формы / HTTP 401 на пульте
- [ ] Нет glassmorphism / неона / bento / «AI dashboard»
- [ ] Имя завода на экране — ПК «Нордщит», не Вольтарис
- [ ] Web не импортирует Prisma / Nest DTO / `apps/api`

## Техника

- [ ] `GET /api/v1/health/ready` 200 только с живой БД
- [ ] `GET /api/v1/demo/links` отдаёт 5 items; неполный каталог — 500, не короткий список
      (D-031, после фичи 18); З-10046 в `items` нет (D-050, после фичи 29)
- [ ] `GET /api/v1/requests/seed-z10043-quote-kuznetsov` 200
- [ ] `GET /api/v1/requests/seed-z10046-live-severnaya-duga` 200, `demoLive: true` (после F29)
- [ ] Неизвестный секрет заявки или пульта — 404 Problem Details, не 401
- [ ] `pnpm check:boundaries`, `lint`, `typecheck`, `test`, `test:packages`, `build` зелёные
- [ ] `pnpm generate:api` и `pnpm db:generate` существуют и гонялись после контракта/схемы
- [ ] Playwright: счастливый путь и битая ссылка
- [ ] Стенд поднимается целями Makefile, не устной инструкцией `docker compose` / `pnpm dev`
