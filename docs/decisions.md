# Решения

| ID    | Область              | Решение                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ----- | -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-001 | Workspace            | Новый репозиторий `client_portal_demo` рядом с Вольтарисом, не fork `apps/*`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| D-002 | Scope пакетов        | Private `@client-portal/*@0.0.0`. Registry и license не выбирались.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D-003 | Mock                 | `mock-core` и отдельный mock-api не входят и не появятся. Заявки = Postgres + seed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D-004 | Каркас API           | Health в заготовке. Предметный MVP — по `docs/llm/feature-NN.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| D-005 | Имя завода           | ПК «Нордщит». Не «Вольтарис», витрину Вольтариса не цитировать как этот продукт.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D-006 | Порты                | Web `http://localhost:3000`, API `3001` (уже в `.env.example`). **Один стенд на машине** для `:3000`/`:3001`: Вольтарис и это демо одновременно не поднимать. Postgres на хосте **5433** (в контейнере 5432), имена compose/container с префиксом `client-portal-`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D-007 | Статусы              | Enum: `accepted` → `in_calculation` → `quote_ready` → `invoice_issued`. Подписи: Принят, В расчёте, КП готово, Счёт выставлен.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| D-008 | Секрет               | Opaque base64url как `generateOpaqueToken`; в БД только `hashOpaqueToken`. Сиды — стабильные fixture из каталога `docs/domain-model.md`. Секрет = capability, не пользователь. Кабинет `/r/{accessSecret}`. Логи и `instance` — D-014.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| D-009 | Файлы                | Только метаданные в seed. Нет upload и нет кнопки «скачать». Имя файла в кабинете — ссылка на HTML-лист (D-029), не download бинарника.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D-010 | Prisma               | Prisma 7 ESM в `apps/api`, `prisma.config.ts`, client `apps/api/src/generated/prisma`, PostgreSQL driver adapter.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| D-011 | Nuxt                 | Nuxt 4 + Tailwind v4 `@theme` + generated `packages/api-client`. Патч Nuxt — фича 3 **без** Tailwind, PostCSS и Vite CSS plugin (зафиксирован в D-022). Пакет Tailwind, CSS-pipeline и `@theme` — фича 4 (D-023). Не угадывать заранее.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D-012 | TDD                  | Тесты по AC до кода; red evidence обязателен. E2E-сценарии пишут в фичах 6–7 до страниц (harness — фича 5). Тест, зелёный до реализации поведения, — false positive.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| D-013 | Lifecycle            | Агентам — цели корневого Makefile, когда они есть. Не сырой `pnpm dev` / `docker compose` в промптах, кроме skill.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| D-014 | Секрет в логах       | Capability в URL: path `/r/{secret}` и `/api/v1/requests/{secret}` **могут** быть в access-логах и в Problem Details `instance`. `detail` — без секрета, SQL и stack. Не логировать секрет/`portalPath` отдельным полем приложения. `serializeHttpRequest` / nestjs-core ради редaction path **не** менять. Тело `GET /demo/links` содержит `portalPath` по контракту — это не лог.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D-015 | Web шрифт/cookie     | IBM Plex Sans — пакет `@fontsource/ibm-plex-sans` (кириллица), не Google Fonts CDN. Сессии нет: cookie не форвардить, `credentials: 'include'` не требуется.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| D-016 | Makefile `up`        | Фича 1: `up` = только Postgres, хост 5433; `dev` = db+api. Фича 3 добавляет web в `dev` (:3000). Фича 8 **расширяет тот же** `up` до web+api+postgres (migrate+seed, затем `--build`); 5433 сохраняется. Второе имя цели полного стенда не заводить. `dev` после F8 останавливает compose `api`/`web` и стартует из compose **только** сервис `postgres`, чтобы хостовые api/web заняли `:3000`/`:3001`. AC фичи 1 («Postgres на 5433») остаётся истинным.                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D-017 | Postgres identity    | Compose project `client-portal-demo`, container `client-portal-postgres`, image `postgres:17`, database/user/password `client_portal`. Host port **5433** (D-006). CI service uses the same credentials on job-network port **5432**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| D-018 | Makefile `verify`    | `verify` = `up` + `db:generate` + `db:migrate` + `generate:api` + `git diff --exit-code` на generated `packages/api-client` (`openapi.json`, `schema.d.ts`) + корневые gates. С фичи 8 после gates — `scripts/compose-smoke.mjs`, затем `pnpm exec playwright install --with-deps chromium` и `pnpm test:e2e` (стенд уже полный; чистый checkout не должен упираться в отсутствие браузера). `make e2e` ставит тот же Chromium. CI по-прежнему делит job `verify` (Postgres service :5432 + gates, без e2e) и job `e2e` (`make up` на :5433; браузер ставит сам job). Сырой `pnpm test` без живой БД падает на HTTP ready=200 — ожидаемо, не регресс фичи 1.                                                                                                                                                                                                                             |
| D-019 | Seed = каталог       | `applyRequestSeed` upsert’ит пять `publicNumber` и удаляет заявки вне каталога (`deleteMany`). Повторный `pnpm db:seed` приводит БД к каталогу. GET `/demo/links` при неизвестном хеше — 500 Problem Details (`RequestFixtureMismatchError` не мапится в 4xx): стенд сломан, усечённый список не отдаём.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| D-020 | Нарезка 3–8          | Оставшийся web не делают одним PR. 3 = Nuxt + `make dev` **без** Tailwind/PostCSS/Vite CSS plugin и без facade; 4 = вся CSS-pipeline + `@theme` + IBM Plex; 5 = Playwright harness; 6 = индекс + первый `useFetch`/facade; 7 = кабинет+404; 8 = compose-smoke + CI e2e. `docs/source-brief.md` не переписывать.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| D-021 | Playwright server    | Smoke F5: `webServer` только Nuxt, `reuseExistingServer: true`. Спеки F6–7 не стартуют второй процесс на `:3000` — стенд `make dev` + seed. CI F8 гоняет e2e против `make up`; в CI `webServer` не передаётся в `defineConfig` (`process.env.CI ? {} : { webServer }`), поэтому второй Nuxt на занятом `:3000` не стартует.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D-022 | Nuxt patch           | Фича 3 фиксирует `nuxt@4.5.2` (npm `latest` на 2026-09-09; engines Node 22.19+ / 24.11+, как у этого workspace). Официальный starter той же линии: `vue@3.5.42`, `vue-router@5.3.1`, `compatibilityDate: '2025-07-15'`. `vue-tsc@3.3.11` — для `nuxt typecheck`. Tailwind/PostCSS/Vite CSS plugin не входят (D-011, D-020).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D-023 | Tailwind / layout    | Фича 4: `tailwindcss@4.3.3` + `@tailwindcss/vite@4.3.3` по [официальному гиду Nuxt](https://tailwindcss.com/docs/installation/framework-guides/nuxt), не PostCSS и не `@nuxtjs/tailwindcss`. `@theme` в `apps/web/app/assets/css/main.css`, подключён из `nuxt.config` `css`. Hex — буквально `docs/frontend.md`. Колонка `--container-document: 42rem` (`max-w-document`; верх диапазона ~40–42rem). IBM Plex: `@fontsource/ibm-plex-sans@5.3.0`, subset `cyrillic`+`latin`, веса 400 и 600. `font-variant-numeric: tabular-nums` для номеров заявок и дат — фичи 6–7, когда появятся эти поверхности. Документ: `app.head.htmlAttrs.lang = 'ru'`. Бренд в шапке и смысловой `h1` страницы — D-040 (UX-нарезка), не откат F4.                                                                                                                                                           |
| D-024 | Playwright smoke     | Фича 5: `@playwright/test@1.63.0` (npm `latest` на 2026-09-09). Конфиг и spec — корень workspace (`playwright.config.ts`, `e2e/`), не `apps/web`: корневой `pnpm test:e2e` / `make e2e` и CI F8 гоняют один набор. Один project `chromium`. Локально `webServer` — только `pnpm --filter @client-portal/web dev` и `reuseExistingServer: true` буквально, не `!process.env.CI`. В CI F8 весь ключ `webServer` не передаётся в `defineConfig` (D-021), поэтому второй Nuxt на занятом `:3000` не стартует.                                                                                                                                                                                                                                                                                                                                                                                |
| D-025 | GitHub remote        | Удалённый GitHub API сегментирован: **read** (PR, diff, checks) — MCP; **write** (создать PR, ревью) — MCP, при 403 PAT сразу `gh` без `mcp_auth` и без второго MCP write. Успешный read не доказывает write. Упавший write не переключает reads. Локальный git — Shell; `fetch`/`pull`/`push` сразу с правами вне sandbox. Политика feature-ветка + PR в `main` не меняется. Ambient «use `gh`» в этом репозитории не действует.                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| D-026 | Compose SSR URL      | Опубликованные порты остаются D-006 (web `:3000`, api `:3001`, Postgres хост **5433**). Внутри Compose API доступен по имени сервиса `api`. Nuxt SSR в контейнере `web` вызывает `http://api:3001/api/v1` (`NUXT_API_BASE_URL` / server `runtimeConfig.apiBaseUrl`). Браузер по-прежнему ходит на `http://localhost:3001/api/v1` (`NUXT_PUBLIC_API_BASE_URL`). `make dev` на хосте оставляет server URL пустым и падает обратно на public localhost. Postgres для контейнера api — `postgres:5432`; host-side migrate/seed — `:5433`.                                                                                                                                                                                                                                                                                                                                                    |
| D-027 | Статусный документ   | Кабинет — статусный документ по ссылке (референс Stripe hosted invoice + USWDS process list), не панель управления. Подпись `Статус заявки`, фраза `Менеджер отправил вам эту ссылку. Вход не нужен.` Плашку `statusLabel` у даты обновления не ставить. Лента: счётчики 1–4, `reachedAt` или `ещё нет`. Файлы — записи метаданных (`kind` → `Опросный лист` / `КП` / `Счёт`, `byteSize` как `Math.round(byteSize / 1000) + ' КБ'`, `uploadedAt`); имена кликабельны по D-029, кнопки «скачать» нет (D-009). Индекс: заголовок `Ссылки для показа` и инструкция `Нажмите строку — откроется экран заказчика по ссылке.`; дисклеймер и подпись про мессенджер не переформулировать. API / OpenAPI / seed не менять. Ссылки с кабинета на `/` нет (индекс служебный). Нарезка кода F9–12. Шапка «текущий шаг = h1» и подписи полей — D-037 (`docs/ux/`), не откат F10.                     |
| D-028 | Makefile `restart`   | `make restart` = `down` + `dev`. `down` после `compose down` вызывает `scripts/free-stand-ports.mjs` на `:3000`/`:3001`/`:5433`. Скрипт убивает только leftover `node`/`nodejs` (Nest/Nuxt). Docker helpers (`com.docker.*`, `vpnkit`, `docker-proxy`) не SIGTERM/SIGKILL: leftover `:5433` снимает compose, не kill PID Docker Desktop. `dev` перед `pnpm dev` освобождает только `:3000`/`:3001`. `make free-ports` — только порты, без compose. Не второе имя полного стенда (D-016).                                                                                                                                                                                                                                                                                                                                                                                                 |
| D-029 | HTML-лист файла      | Имя вложения в кабинете — настоящая ссылка на `/r/{accessSecret}/d/{fileName}` (`encodeURIComponent` в `href`). Лист — тот же документный layout: тип, имя, номер, контрагент, `title`, размер, `uploadedAt`, зачин по `kind` (`questionnaire` → `Исходные требования.`; `quote` → `Коммерческое предложение.`; `invoice` → `Счёт.`), строки спецификации заявки. Ссылка `К заявке {publicNumber}` → `/r/{accessSecret}`. Индекс `/` не связывать. Неизвестный секрет — тупик `Ссылка недействительна`, HTTP 404, без логина. Нет PDF, upload, кнопки «скачать», `[download]`, `href="#"`. API / OpenAPI / Prisma / seed не менять. Разные тексты «нет заявки» / «нет документа» и выписка на листе — D-039 (`docs/ux/`).                                                                                                                                                                |
| D-030 | Нарезка дефектов     | Дефекты после ревью `main` (кабинет + API + стенд) закрывают **только** фичи 15–27 по `docs/remediation-plan.md`. Оператор в новом чате пишет `выполни задачу N` — это полный вход: агент читает `docs/llm/feature-NN.md` из таблицы плана, не додумывает соседние задачи. Фича 14 — docs-only (этот ряд + промпты). Код продукта в F14 не менять. **Не входит ни в одну задачу:** новые CSPRNG-секреты сидов (D-008), логин/auth на `/` и `GET /demo/links`, HTTPS, Redis, модуль `nuxt-security`, смена D-014, SWR/ISR на `/r/**`, правка пароля Postgres (D-017), Helmet CSP на JSON API, проверка `status`↔`stageHistory` на чтении, `formatByteSize` для `< 500` байт, смена Vue `:key` на спеке/файлах. UX/UI понятности — не этот ряд, а D-036 / D-042 и `docs/ux/` (`выполни ux задачу N` или `реализуй ux задачу N`, не эта фраза). Следующий свободный ID после D-046 — D-047. |
| D-031 | Каталог fail-closed  | D-019 остаётся: неизвестный хеш → 500, не усечённый список. Дополнительно: `GET /demo/links` 500 `RequestFixtureMismatchError`, если в БД нет **всех** пяти `publicNumber` каталога (включая пустую таблицу). Успех — ровно каталог 1:1. Empty state «Заявок пока нет» на индексе не удалять в API-фиче; после F18 API больше не отдаёт `[]` на предназначенном стенде. Сиды и fixture-секреты не менять.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| D-032 | Заголовки capability | Страницы `/`, `/r/**` и JSON `GET /demo/links`, `GET /requests/{accessSecret}`: `Cache-Control: private, no-store`. Web (Nuxt `routeRules`, не модуль `nuxt-security`): ещё `Referrer-Policy: no-referrer`, `X-Robots-Tag: noindex, nofollow`, `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`. Health (`/health/live`, `/health/ready`) не ужесточать cache. D-014 не менять: path с секретом в `instance`/access-логе допустим. Не включать Nitro `swr`/`isr` на `/r/**`.                                                                                                                                                                                                                                                                                                                                                                                                   |
| D-033 | Host bind loopback   | В `compose.yaml` опубликованные порты — `127.0.0.1:3000:3000`, `127.0.0.1:3001:3001`, `127.0.0.1:5433:5432`. Номера портов и D-006 не менять. Сеть Compose (`api:3001`, `postgres:5432`) без bind. CI e2e на `localhost` остаётся валидным. Не закрывать `make dev` хостовые api/web (они и так на машине ведущего).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| D-034 | NuxtLink после watch | `NuxtLink` на индексе, имени файла и «К заявке» — **только** фича 25 и только после фичи 16 в `main`. Фича 16 чинит реактивный ключ/`watch` `useRequestPortal` и **не** меняет `<a href>`. Иначе клиентская навигация покажет чужую заявку. Prefetch не обязателен. Cookie / `credentials: 'include'` по-прежнему запрещены (D-015).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| D-035 | Имя файла уникально  | В Prisma `RequestFile`: `@@unique([requestId, fileName])`. Сиды уже уникальны — состав каталога не менять. Lookup листа по `fileName` остаётся на web. Миграция только forward-only. OpenAPI envelope не менять.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D-036 | UX-нарезка           | Доработки понятности UI после разбора кабинета закрывают **только** `docs/ux/task-NN.md`. Оператор задач 1–12: `выполни ux задачу N` → [`docs/ux/README.md`](ux/README.md). Фраза `выполни задачу N` без `ux` — только D-030 / `docs/remediation-plan.md`. Это не фичи 15–27 и не `docs/llm/feature-NN.md`. Код в docs-PR нарезки не писать. Трек не блокирует F15–27 и ими не блокируется; при пересечении Vue — rebase. **Не входит ни в одну UX-задачу:** оплата, чат, OTP, телефон, логин, «скачать», ссылка кабинета на `/`, перефразировка дисклеймера/мессенджера на индексе, API/OpenAPI/Prisma/seed, glass/bento/неон/UI-kit, смена D-014, Redis, `nuxt-security`, SWR/ISR на `/r/**`. `NuxtLink` не добавлять (D-034); уже поставленный F25 не откатывать. Задачи 13–18 и оператор `реализуй ux задачу N` — D-042.                                                             |
| D-037 | Шапка кабинета       | Подпись `Статус заявки` стоит над текущим `stages[].label` (`status === request.status`) — это единственный `h1` кабинета. `publicNumber` виден (tabular), не heading. Фраза про ссылку без входа остаётся. Подписи: `Заказчик`, `Изделие`, `Обновлено`. Одна фраза «что дальше»: accepted `Заявку приняли. Сейчас готовят расчёт.`; in_calculation `Идёт расчёт. Коммерческое предложение ещё не готово.`; quote_ready `Коммерческое предложение готово. Счёт ещё не выставлен.`; invoice_issued `Счёт выставлен. Оплата в этом окне не принимается.` Title: `{statusLabel} — {publicNumber} — ПК «Нордщит»`. Штамп в ленте остаётся (label в шапке + ленте). Плашки у даты обновления нет (D-027).                                                                                                                                                                                     |
| D-038 | Лента и 390px        | Лента остаётся `<ol aria-label="Этапы заявки">`, не wizard. Текущий `li`: `aria-current="step"` и `sr-only` `сейчас`. Будущие — `ещё нет`. На ширине `< 40rem` пункт колонка (счётчик+штамп / дата); спека — блоки с видимыми подписями `Наименование` / `Кол-во` / `Ед.` / `Комментарий`, без горизонтального скролла страницы и без обрезанных заголовков. Desktop-таблица фичи 11 сохраняется от `40rem`. Не канбан и не прогресс-бар магазина.                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| D-039 | Лист и тупики        | Под «Файлы»: `Имя файла открывает выписку на экране.` Лист: дисклеймер `Это выписка на экране, а не файл для скачивания.`; зачины D-029; подписи `Заказчик` / `Изделие` / `Загружено` / `Размер`; спека с `quantity`/`unit`/`comment`; title `{kindLabel} — {publicNumber} — ПК «Нордщит»`. Неизвестный секрет: текст `Заявки по этой ссылке нет. Проверьте адрес или попросите новую ссылку у менеджера.` без ссылки в кабинет. Заявка есть, файла нет: `Такого документа в заявке нет. Откройте заявку и выберите документ из списка.` + `К заявке {publicNumber}`. Оба — h1 `Ссылка недействительна`, HTTP 404. На 404 не показывать `Код ошибки` / UUID. Нет download.                                                                                                                                                                                                               |
| D-040 | Хром документа       | Бренд шапки `ПК «Нордщит»` — не heading. Один смысловой `h1` на страницу (индекс `Ссылки для показа`; кабинет — D-037; лист — `fileName`; 404 — `Ссылка недействительна`). Title индекса `Ссылки для показа — ПК «Нордщит»`; 404 `Ссылка недействительна — ПК «Нордщит»`. `:focus-visible` — `outline` 2px solid `--color-accent`, offset 2px; не `outline: none` без замены. Штамп статуса — weight 400 (не semibold «кнопка»). Запрет «не переносить в ленту» снят: после языка ссылки D-044 штамп должен отличаться от ссылки и выглядеть ненажимаемым и на индексе, и на текущем шаге ленты (D-045).                                                                                                                                                                                                                                                                                 |
| D-041 | Prisma vendor skills | Вендорные skills [`prisma/skills`](https://github.com/prisma/skills) (MIT) фиксирует `apps/api/skills-lock.json`; канон — `apps/api/.agents/skills/` (`prisma-cli`, `prisma-client-api`, `prisma-database-setup`, `prisma-upgrade-v7` и остальные пакеты инсталлятора). `apps/api/.claude/skills/` и `apps/api/.windsurf/skills/` — симлинки на тот же канон, не второй источник. Процедуры репозитория остаются в корневом `.agents/skills/`. Вендор **не** перекрывает `prisma-persistence-boundary`, D-010, Makefile (D-013) и product-scope. **Не поставка этого демо:** `prisma-compute`, `prisma-postgres` / `prisma-postgres-setup` (Prisma Postgres cloud), `prisma-mongodb-upgrade`, `prisma-driver-adapter-implementation`. Вендорные файлы не править вручную и не прогонять через Prettier. Обновление — инсталлятор + lock.                                                 |
| D-042 | UX сетка и ссылки    | После задач 1–12 внутреннюю сетку листа и покойное состояние ссылок закрывают **только** `docs/ux/task-13.md` … `task-18.md`. Оператор: `реализуй ux задачу N` → [`docs/ux/README.md`](ux/README.md) + skill `implement-review-cycle` (родитель не пишет код). Отдельно писать `через implement → review` не нужно. `выполни ux задачу N` без `реализуй` — по-прежнему один чат (задачи 1–12). `выполни задачу N` / `реализуй задачу N` без `ux` — не этот каталог. Код в docs-PR нарезки не писать. Задача 12 в `main` до старта 13. Трек не блокирует F15–27. Исключения — как в D-036. Контракт вида — D-043.                                                                                                                                                                                                                                                                         |
| D-043 | Ссылка и сетка листа | Класс `.document-link` в `apps/web/app/assets/css/main.css`: в покое `color` и `text-decoration-color` = `--color-accent` (`#3d5a73` / `rgb(61, 90, 115)`), underline. Не `--color-rule` в покое. Hover может уходить в `--color-ink`. `:focus-visible` D-040 не ослаблять. Штампы и тип файла классом не красить. Список файлов — одна сетка на все строки (одинаковый `grid-template-columns` или `display: contents` в grid `ul`): ≥40rem четыре колонки тип/имя/размер/дата; <40rem две общие колонки, X имён и дат совпадают. Лента ≥40rem — три колонки счётчик/штамп/дата, X дат совпадают; <40rem колонка D-038. Спека листа — схема кабинета (таблица ≥40rem, блоки <40rem), не `inline`+`ml-*`. «Кол-во» в `thead` кабинета на desktop — одна строка. Не UI-kit. Начертание покоя — D-044.                                                                                     |
| D-044 | Начертание ссылки    | Покой `.document-link` (поверх D-043): типографика, не кнопка и не поле. Линия `text-decoration-thickness: 2px`, `text-underline-offset: 3px` (язык GOV.UK links: толщина и отступ, не рамка). Лёгкая плашка `color-mix(--color-accent 10%, transparent)` и `box-decoration-break: clone` — маркер по строкам переноса, не один прямоугольник. `display: inline`, без `border`. Hover: ink и линия 3px. «К заявке» может иметь декоративную `←` (`aria-hidden`), без смены accessible name. Штамп — D-045. Не UI-kit и не кнопка «скачать». `:focus-visible` D-040 не ослаблять.                                                                                                                                                                                                                                                                                                         |
| D-045 | Штамп ≠ ссылка       | Класс `.status-stamp`: `color` = `--color-ink` (`#1c1917` / `rgb(28, 25, 23)`), фон `--color-rule` (`#d6d0c4` / `rgb(214, 208, 196)`), `font-weight: 400`, без underline и без акцента, `cursor: default` (не `inherit`: над штампом не pointer, даже внутри строки-ссылки индекса). Это NHS tag, не контрол: не `.document-link`, не `bg-accent/15 text-accent`. Индекс: штамп остаётся внутри строки-ссылки (клик по штампу = клик по строке; курсор всё равно `default`, чтобы штамп не выглядел нажимаемым). Кабинет: текущий шаг ленты — тот же класс; пройденные ink без плашки, будущие muted. Не solid-dark «кнопка».                                                                                                                                                                                                                                                            |
| D-046 | Русский UI           | Канон зрительских фраз: под «Файлы» `Имя файла открывает выписку на экране.`; дисклеймер листа `Это выписка на экране, а не файл для скачивания.`; тупик чужого файла `Такого документа в заявке нет. Откройте заявку и выберите документ из списка.` Комментарий З-10043 `IP54, навесное` (согласование с «устройство»). Уточняет D-039 и текст каталога; состав строк сида не менять. Дисклеймер индекса (D-027) не перефразировать. Штампы D-007 (`Принят` / `В расчёте` / `КП готово` / `Счёт выставлен`) остаются именами шагов, не согласованием с «заявка».                                                                                                                                                                                                                                                                                                                       |
