# Frontend

Интерфейс на русском. Два экрана, один layout. Не витрина, не дашборд.

## Живые референсы

Проверены fetch'ем в чате плана (2026-09-09). Не картинки из нейросетей.

| URL                                                                             | Открылся | Что берём                                                                           | Что не берём                                    |
| ------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------- | ----------------------------------------------- |
| [Stripe hosted invoice](https://docs.stripe.com/invoicing/hosted-invoice-page)  | да       | уникальный длинный URL, документ (номер, статус, файлы списком), воздух, не графики | оплату, email-login, branding-SaaS CTA          |
| [USWDS Process list](https://designsystem.digital.gov/components/process-list/) | да       | вертикальные 3–10 шагов, параллельные короткие заголовки, счётчики как штампы       | wizard/step-indicator «заполните форму», канбан |
| [NHS Tag](https://service-manual.nhs.uk/design-system/components/tag)           | да       | статус = некликабельный штамп, прилагательные, тёмный текст на светлой плашке       | тег-кнопка, UPPERCASE как кнопка                |

Замены (не записывать как открытый экран):

- GOV.UK Design System (`/styles/colour/`, `/components/tag/`, `/components/task-list/`):
  `WebFetch` timeout. Идея близка NHS/USWDS; в токены GOV.UK hex не копировать.
- [AfterShip Tracking marketing](https://www.aftership.com/tracking): открылся, это
  SaaS-маркетинг — не референс UI.
- [AfterShip branded tracking help](https://support.aftership.com/en/tracking/articles/15441577-set-up-and-customize-the-branded-tracking-page):
  открылся. Берём только IA «tracking results + timeline». Отбрасываем chat, Instagram,
  product recommendations, AI EDD.

## Что запрещаем (нейрослоп и соседние соблазны)

Glassmorphism, градиентные стеклянные карточки, bento-сетка, неон, 3D-блобы, тёмная
«кибер» тема, стоковые иллюстрации «AI dashboard», канбан, графики KPI, hero-баннер
магазина, бесконечный UI-kit.

## Токены

Одна гарнитура: **IBM Plex Sans** (кириллица) + `font-variant-numeric: tabular-nums` для
номеров заявок и дат. Грузить пакет `@fontsource/ibm-plex-sans`, не Google Fonts CDN
(D-015). Не Inter как «дефолт SaaS», не декоративный serif.

Предлагаемые значения для `@theme` (фича 3 фиксирует в CSS, не копирует токены Вольтариса):

- бумага: `--color-paper: #f4f1ea`
- поверхность листа: `--color-sheet: #fffcf7`
- чернила: `--color-ink: #1c1917`
- вторичный текст: `--color-ink-muted: #5c564e`
- акцент (пыльная сталь / синий): `--color-accent: #3d5a73`
- линия: `--color-rule: #d6d0c4`
- штамп текущий: акцент + светлая плашка
- штамп пройден: чернила, без кричащего фона
- штамп будущий: muted

Много воздуха: узкая колонка документа (~40–42rem), крупные поля, иерархия заголовок →
номер → штампы → спецификация → файлы.

## Экран 1 — индекс демо-ссылок (`/`)

Служебный. Сверху явным текстом: этот список не показывается заказчику. Подпись в духе
«так выглядит то, что вы отправили бы заказчику в мессенджер».

Список из API (`GET /demo/links`): номер, контрагент, статус-штамп, дата. Каждая строка —
настоящая ссылка на `portalPath`. Нет кнопки «скопировать», нет `#`.

Состояния: loading / error (`traceId`) / empty (если seed пуст — честный empty, не фейк).

## Экран 2 — кабинет (`/r/{accessSecret}`)

Шапка: ПК «Нордщит», номер заявки, дата обновления. Лента четырёх штампов. Спецификация
таблицей/списком строк. Файлы — перечень имён без кнопки «скачать» (нет бинарника; мёртвый
CTA запрещён).

404: заголовок в духе «Ссылка недействительна», короткое объяснение, без формы входа,
телефона, OTP.

Нет чата, логина, корзины, графиков.
