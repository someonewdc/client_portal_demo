# План UX/UI (понятность, self-explained)

Вход оператора. Код здесь не пишут. Каждая задача — отдельный чат, одна
feature-ветка, один PR в `main` по `docs/ux/task-NN.md`.

Это **не** скоуп фич 15–27 и **не** `docs/llm/feature-NN.md`. Дефекты API/стенда
по-прежнему: [`../remediation-plan.md`](../remediation-plan.md)
(`выполни задачу N`, без `ux`).

## Как запускать

В новом чате достаточно фразы:

```text
выполни ux задачу N
```

`N` — номер из колонки «Задача» ниже. Это полный вход: агент открывает указанный
`docs/ux/task-NN.md` и больше ничего у тебя не спрашивает. Не скармливай прошлый
анализ и не уточняй scope.

Фраза `выполни задачу N` без слова `ux` — **не** этот каталог, а
[`../remediation-plan.md`](../remediation-plan.md) (фичи 15–27).

Без `через implement → review` это один чат: агент пишет код сам. Цикл
(родитель не пишет) — только по явной просьбе, skill `implement-review-cycle`:

```text
выполни ux задачу N через implement → review
```

Голые «следующий пункт» и «суперагент» цикл не включают.

Задачу 0 (этот набор документов) повторно не запускай после merge.

## Порядок

Задача N должна быть в `main` до старта N+1. Фичи 15–27 этот трек не блокируют и
им не мешают: при пересечении тех же Vue — rebase на `main`. `NuxtLink` не
добавлять (D-034); если фича 25 уже в `main`, существующий `NuxtLink` не
откатывать.

```text
docs (D-036…D-040, этот каталог)
  → 1  focus-visible
  → 2  иерархия заголовков + title индекса/404
  → 3  кабинет: статус в шапке
  → 4  кабинет: подписи полей
  → 5  кабинет: что дальше
  → 6  лента: текущий шаг
  → 7  лента на 390px
  → 8  спецификация на 390px
  → 9  файлы: жест «лист»
  → 10 лист файла: выписка
  → 11 тупик файла ≠ тупик заявки
  → 12 индекс: штамп не кнопка
```

## Таблица задач

| Задача | Промпт                     | Слой      | Зачем                                     |
| ------ | -------------------------- | --------- | ----------------------------------------- |
| 1      | [`task-01.md`](task-01.md) | css + e2e | Клавиатурный фокус виден                  |
| 2      | [`task-02.md`](task-02.md) | layout    | Бренд не h1; смысловой h1 на страницах    |
| 3      | [`task-03.md`](task-03.md) | кабинет   | Текущий шаг сразу в шапке документа       |
| 4      | [`task-04.md`](task-04.md) | кабинет   | Заказчик / изделие / обновлено подписаны  |
| 5      | [`task-05.md`](task-05.md) | кабинет   | Одна фраза «что дальше» по статусу        |
| 6      | [`task-06.md`](task-06.md) | лента     | `aria-current` и «сейчас» у текущего шага |
| 7      | [`task-07.md`](task-07.md) | лента     | Счётчик, штамп и дата читаются на 390px   |
| 8      | [`task-08.md`](task-08.md) | спека     | Заголовки таблицы не обрезаются на 390px  |
| 9      | [`task-09.md`](task-09.md) | файлы     | Имя открывает выписку, не «скачать PDF»   |
| 10     | [`task-10.md`](task-10.md) | лист      | Выписка с полной спекой, не заготовка     |
| 11     | [`task-11.md`](task-11.md) | 404       | Нет документа ≠ нет заявки                |
| 12     | [`task-12.md`](task-12.md) | индекс    | Штамп статуса не выглядит как кнопка      |

## Что сознательно не чиним

Зафиксировано в D-036. Агент задачи N не «доделывает» этот список:

- оплату, чат, OTP, телефон, логин, кнопку «скачать», `href="#"`;
- ссылку с кабинета на служебный индекс `/`;
- переформулировку дисклеймера и подписи про мессенджер на `/`;
- API / OpenAPI / Prisma / seed / fixture-секреты;
- фичи 15–27 из `docs/remediation-plan.md`;
- glassmorphism, bento, неон, UI-kit, канбан, графики;
- смену D-014, Redis, `nuxt-security`, SWR/ISR на `/r/**`.

## Решения

D-036 (трек), D-037 (шапка кабинета), D-038 (лента), D-039 (лист и 404),
D-040 (хром: h1, title, focus) — `docs/decisions.md`.
